"""Poker statistics recording for the tank simulation.

Aggregates poker game outcomes into ecosystem statistics. Import the concrete
submodule directly (e.g.
``from core.poker.stats.poker_stats_manager import PokerStatsManager``); this
package does not re-export, keeping it off the ``core.poker`` facade load chain
(see ADR-008).
"""
